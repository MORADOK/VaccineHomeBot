# คู่มือการตรวจสอบและแก้ไขวันนัดวัคซีน

## ภาพรวม

เอกสารนี้อธิบายวิธีการตรวจสอบและแก้ไขวันนัดวัคซีนที่อาจคำนวณผิดในฐานข้อมูล

## เครื่องมือที่มี

### 1. สคริปต์ตรวจสอบ (verify-appointment-dates.js)
ตรวจสอบว่านัดที่มีอยู่ในฐานข้อมูลคำนวณถูกต้องหรือไม่

### 2. สคริปต์แก้ไข (fix-appointment-dates.js)
แก้ไขนัดที่คำนวณผิดให้ถูกต้อง

## วิธีการใช้งาน

### ขั้นตอนที่ 1: ตั้งค่า Environment Variables

สร้างไฟล์ `.env` (ถ้ายังไม่มี) และเพิ่ม:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### ขั้นตอนที่ 2: ตรวจสอบนัดทั้งหมด

รันคำสั่ง:

```bash
npm run verify-appointments
```

สคริปต์จะ:
1. ดึงข้อมูลวัคซีนทั้งหมดจาก `vaccine_schedules`
2. ดึงข้อมูลนัดทั้งหมดจาก `appointments`
3. คำนวณวันนัดที่ถูกต้องจากเข็มแรก + ระยะห่างสะสม
4. เปรียบเทียบกับวันนัดจริงในฐานข้อมูล
5. แสดงผลการตรวจสอบ
6. สร้างไฟล์รายงาน `appointment-verification-report.json`

### ตัวอย่างผลลัพธ์:

```
🔍 เริ่มตรวจสอบความถูกต้องของวันนัดในฐานข้อมูล...

📊 พบวัคซีน 5 ประเภท
📅 พบนัดทั้งหมด 150 รายการ
👥 พบผู้ป่วย 50 คน (แยกตามวัคซีน)

====================================================================================================

👤 ผู้ป่วย: สมชาย ใจดี
💉 วัคซีน: วัคซีนไข้หวัดใหญ่ (Influenza)
📅 เข็มแรก: 2025-01-01
✅ ฉีดแล้ว: 2/3 เข็ม
📆 นัดที่มี: 1 นัด

  ✅ โดสที่ 1: 2025-01-01 (ถูกต้อง) [completed]
  ✅ โดสที่ 2: 2025-01-08 (ถูกต้อง) [completed]
  ❌ โดสที่ 3: 2025-02-05 (ควรเป็น 2025-01-29) [ต่าง 7 วัน] [scheduled]

====================================================================================================

📊 สรุปผลการตรวจสอบ

✅ นัดที่ถูกต้อง: 145 รายการ (96.7%)
❌ นัดที่ไม่ถูกต้อง: 5 รายการ (3.3%)
📊 ตรวจสอบทั้งหมด: 150 รายการ

====================================================================================================

❌ รายการนัดที่ไม่ถูกต้อง:

1. สมชาย ใจดี (P001)
   วัคซีน: Influenza - โดสที่ 3
   วันที่จริง: 2025-02-05
   วันที่ควรเป็น: 2025-01-29
   ต่างกัน: 7 วัน
   สถานะ: scheduled
   ID: abc123

📄 บันทึกรายงานไว้ที่: appointment-verification-report.json

✅ ตรวจสอบเสร็จสิ้น
```

### ขั้นตอนที่ 3: ตรวจสอบรายงาน

เปิดไฟล์ `appointment-verification-report.json` เพื่อดูรายละเอียด:

```json
{
  "timestamp": "2025-11-17T08:30:00.000Z",
  "summary": {
    "total_checked": 150,
    "total_correct": 145,
    "total_incorrect": 5,
    "accuracy_percentage": "96.7"
  },
  "incorrect_appointments": [
    {
      "patient_name": "สมชาย ใจดี",
      "patient_id": "P001",
      "vaccine_type": "Influenza",
      "dose_number": 3,
      "actual_date": "2025-02-05",
      "expected_date": "2025-01-29",
      "difference_days": 7,
      "status": "scheduled",
      "appointment_id": "abc123"
    }
  ]
}
```

### ขั้นตอนที่ 4: แก้ไขนัดที่ผิด (ถ้าต้องการ)

⚠️ **คำเตือน**: สคริปต์นี้จะแก้ไขข้อมูลในฐานข้อมูล กรุณาสำรองข้อมูลก่อน!

รันคำสั่ง:

```bash
npm run fix-appointments
```

สคริปต์จะ:
1. อ่านรายงานจาก `appointment-verification-report.json`
2. แสดงรายการนัดที่จะแก้ไข
3. ขอยืนยันจากผู้ใช้
4. แก้ไขวันนัดในฐานข้อมูล
5. เพิ่มหมายเหตุว่าแก้ไขอัตโนมัติ

### ตัวอย่างการใช้งาน:

```bash
$ npm run fix-appointments

🔧 สคริปต์แก้ไขวันนัดที่คำนวณผิด

⚠️  คำเตือน: สคริปต์นี้จะแก้ไขข้อมูลในฐานข้อมูล
⚠️  กรุณาสำรองข้อมูลก่อนใช้งาน

พบนัดที่ต้องแก้ไข: 5 รายการ

1. สมชาย ใจดี - Influenza โดสที่ 3
   จาก: 2025-02-05 → เป็น: 2025-01-29 (scheduled)

2. สมหญิง รักดี - COVID-19 โดสที่ 2
   จาก: 2025-01-20 → เป็น: 2025-01-15 (scheduled)

...

คุณต้องการแก้ไขนัดเหล่านี้หรือไม่? (yes/no): yes

🔧 เริ่มแก้ไขนัด...

✅ แก้ไข: สมชาย ใจดี - โดสที่ 3 (2025-02-05 → 2025-01-29)
✅ แก้ไข: สมหญิง รักดี - โดสที่ 2 (2025-01-20 → 2025-01-15)
...

====================================================================================================

📊 สรุปผลการแก้ไข

✅ แก้ไขสำเร็จ: 5 รายการ
❌ แก้ไขไม่สำเร็จ: 0 รายการ
📊 ทั้งหมด: 5 รายการ

✅ เสร็จสิ้น
```

## หลักการคำนวณ

สคริปต์ใช้หลักการคำนวณแบบเดียวกับระบบหลัก:

```
เข็มที่ 1: วันที่ 1 มกราคม (ฐาน)
เข็มที่ 2: วันที่ 1 มกราคม + 7 วัน = 8 มกราคม
เข็มที่ 3: วันที่ 1 มกราคม + (7+21) วัน = 29 มกราคม

✅ คำนวณจากเข็มแรกเสมอ
❌ ไม่นับต่อจากเข็มล่าสุด
```

## การตรวจสอบแบบ Manual

หากต้องการตรวจสอบด้วยตนเอง:

### 1. เข้า Supabase Dashboard
- ไปที่ Table Editor → appointments
- Filter ตามผู้ป่วยและวัคซีน

### 2. ตรวจสอบวันนัด
- หาวันที่ฉีดเข็มแรก (first dose)
- ดูระยะห่างจาก vaccine_schedules
- คำนวณวันนัดที่ถูกต้อง
- เปรียบเทียบกับวันนัดในฐานข้อมูล

### 3. แก้ไขด้วยตนเอง (ถ้าต้องการ)
- คลิก Edit บนแถวที่ต้องการแก้ไข
- เปลี่ยน appointment_date
- เพิ่มหมายเหตุใน notes
- Save

## คำถามที่พบบ่อย

### Q: ทำไมนัดถึงคำนวณผิด?
A: อาจเกิดจาก:
- ระบบเก่าคำนวณจากเข็มล่าสุด (ไม่ใช่เข็มแรก)
- การป้อนข้อมูลด้วยตนเองผิดพลาด
- การเปลี่ยนแปลง dose_intervals ใน vaccine_schedules

### Q: ควรแก้ไขนัดที่ผ่านมาแล้วหรือไม่?
A: ไม่แนะนำ เพราะ:
- นัดที่ completed แล้วคือข้อมูลจริงที่เกิดขึ้น
- ควรแก้ไขเฉพาะนัดที่ scheduled/pending

### Q: ถ้าผู้ป่วยฉีดล่าช้าจะเกิดอะไรขึ้น?
A: ระบบจะ:
- บันทึกวันที่ฉีดจริง (ไม่ใช่วันที่คำนวณ)
- คำนวณเข็มถัดไปจากเข็มแรก (ไม่ใช่เข็มล่าสุด)
- วันนัดจะยังคงถูกต้องตามแผน

### Q: สคริปต์ปลอดภัยหรือไม่?
A: ใช่ เพราะ:
- ขอยืนยันก่อนแก้ไข
- เพิ่มหมายเหตุทุกครั้งที่แก้ไข
- สามารถ rollback ได้ถ้ามี backup

### Q: ควรรันสคริปต์บ่อยแค่ไหน?
A: แนะนำ:
- หลังจาก migrate ข้อมูลจากระบบเก่า
- เมื่อเปลี่ยนแปลง vaccine_schedules
- ทุก 1-3 เดือน เพื่อตรวจสอบความถูกต้อง

## การสำรองข้อมูล

ก่อนรันสคริปต์แก้ไข ควรสำรองข้อมูล:

### วิธีที่ 1: ผ่าน Supabase Dashboard
1. ไปที่ Table Editor → appointments
2. Export to CSV
3. เก็บไฟล์ไว้

### วิธีที่ 2: ผ่าน SQL
```sql
-- สำรองตาราง appointments
CREATE TABLE appointments_backup AS 
SELECT * FROM appointments;
```

### วิธีที่ 3: ผ่าน pg_dump
```bash
pg_dump -h your-host -U postgres -t appointments > appointments_backup.sql
```

## การ Rollback

หากต้องการย้อนกลับ:

### วิธีที่ 1: จากไฟล์ backup
```sql
-- ลบข้อมูลปัจจุบัน
DELETE FROM appointments;

-- นำเข้าข้อมูลจาก backup
\copy appointments FROM 'appointments_backup.csv' CSV HEADER;
```

### วิธีที่ 2: จากตาราง backup
```sql
-- ลบข้อมูลปัจจุบัน
DELETE FROM appointments;

-- คัดลอกจาก backup
INSERT INTO appointments 
SELECT * FROM appointments_backup;
```

## การติดต่อสนับสนุน

หากพบปัญหาหรือมีคำถาม:
1. ตรวจสอบ logs ใน console
2. ดูไฟล์รายงาน appointment-verification-report.json
3. ติดต่อทีมพัฒนา

---

**เอกสารนี้สร้างเมื่อ**: 17 พฤศจิกายน 2025  
**เวอร์ชัน**: 1.0.0  
**ผู้ดูแล**: VCHome Hospital Development Team
