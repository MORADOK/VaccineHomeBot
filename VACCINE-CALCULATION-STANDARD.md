# มาตรฐานการคำนวณวันนัดวัคซีน - VCHome Hospital

## 📋 หลักการคำนวณที่ถูกต้อง

ระบบคำนวณวันนัดวัคซีนโดยใช้หลักการ **นับจากเข็มแรกเป็นฐาน** ตามมาตรฐานทางการแพทย์

### สูตรการคำนวณ:
```
วันนัดเข็มที่ N = วันที่เข็มแรก + dose_intervals[N-2]
```

**หมายเหตุ:** `dose_intervals` เก็บระยะห่าง**จากเข็มแรก**ไปยังแต่ละเข็ม (**ไม่ใช่ระยะห่างระหว่างเข็มติดกัน**)

---

## 📊 ตารางวัคซีนในระบบ (ตาม vaccine_schedules)

| วัคซีน | vaccine_type | จำนวนเข็ม | dose_intervals | คำอธิบาย |
|--------|--------------|-----------|----------------|----------|
| วัคซีนพิษสุนัขบ้า | rabies | 5 | ⚠️ `[3,4,7,14]` | **ผิด! ควรแก้เป็น `[3,7,14,28]`** |
| วัคซีนป้องกันบาดทะยัก | tetanus | 3 | ✅ `[28,168]` | ถูกต้อง |
| วัคซีนป้องกันมะเร็งปากมดลูก | hpv | 3 | ✅ `[28,140]` | ถูกต้อง |
| วัคซีนไวรัสตับอักเสบบี | hep_b | 3 | ✅ `[28,140]` | ถูกต้อง |
| วัคซีนงูสวัด | shingles | 2 | ✅ `[84]` | ถูกต้อง (12 สัปดาห์) |
| วัคซีนปอดอักเสบ | pneumonia | 2 | ✅ `[56]` | ถูกต้อง (8 สัปดาห์) |
| วัคซีนอีสุกอีใส | chickenpox | 2 | ✅ `[28]` | ถูกต้อง (4 สัปดาห์) |
| วัคซีนไข้หวัดใหญ่ | flu | 2 | ✅ `[365]` | ถูกต้อง (ปีละครั้ง) |

---

## ⚠️ ปัญหาวัคซีนพิษสุนัขบ้า

### ข้อมูลปัจจุบันในฐานข้อมูล: `[3,4,7,14]` ❌

ตัวเลขนี้หมายถึง **ระยะห่างระหว่างเข็มติดกัน** ซึ่งผิดจากมาตรฐานที่กำหนด:

```
เข็มที่ 1: วันที่ 0
เข็มที่ 2: วันที่ 0 + 3 = 3
เข็มที่ 3: วันที่ 3 + 4 = 7   ← นับจากเข็มก่อนหน้า (ผิด!)
เข็มที่ 4: วันที่ 7 + 7 = 14
เข็มที่ 5: วันที่ 14 + 14 = 28
```

### ข้อมูลที่ถูกต้องตามมาตรฐาน WHO: `[3,7,14,28]` ✅

```
เข็มที่ 1: วันที่ 0 (วันแรก)
เข็มที่ 2: วันที่ 0 + 3 = 3   ← ห่างจากเข็มแรก 3 วัน
เข็มที่ 3: วันที่ 0 + 7 = 7   ← ห่างจากเข็มแรก 7 วัน
เข็มที่ 4: วันที่ 0 + 14 = 14 ← ห่างจากเข็มแรก 14 วัน
เข็มที่ 5: วันที่ 0 + 28 = 28 ← ห่างจากเข็มแรก 28 วัน
```

### 🔧 วิธีแก้ไข:

อัปเดตข้อมูลในตาราง `vaccine_schedules`:

```sql
UPDATE vaccine_schedules
SET dose_intervals = '[3,7,14,28]'
WHERE vaccine_type = 'rabies';
```

---

## 💻 การใช้งานในโค้ด

### ✅ โค้ดที่ถูกต้อง (ใช้งานในระบบปัจจุบัน)

#### 1. NextAppointments.tsx (บรรทัด 248-259)
```typescript
// Calculate from the FIRST dose date
const firstDoseDate = new Date(patient.first_dose_date);

// Get the interval for the NEXT dose (not cumulative)
const nextDoseIntervalDays = intervals[patient.doses_received];

// Calculate next dose date from first dose + interval for this specific dose
const nextDoseDate = new Date(firstDoseDate.getTime());
nextDoseDate.setDate(firstDoseDate.getDate() + nextDoseIntervalDays);
```

#### 2. FullDoseScheduleModal.tsx (บรรทัด 96-139)
```typescript
// Calculate from first dose + individual interval for this dose
const baseFirstDoseDate = new Date(firstDoseDate);
const intervalDays = i === 0 ? 0 : (intervals[i - 1] || 0);

const calculatedDate = new Date(baseFirstDoseDate.getTime());
calculatedDate.setDate(baseFirstDoseDate.getDate() + intervalDays);
```

**หมายเหตุ:** โค้ดทั้ง 2 ส่วนคำนวณถูกต้องตามมาตรฐาน โดย `intervals[i]` คือระยะห่างจากเข็มแรกไปยังเข็มที่ i+1

---

## 📖 ตัวอย่างการคำนวณ

### ตัวอย่างที่ 1: วัคซีนบาดทะยัก (3 เข็ม)

**ข้อมูล:**
- เข็มแรก: 1 มกราคม 2025
- dose_intervals: `[28, 168]`

**การคำนวณ:**
```
เข็มที่ 1: 1 มกราคม 2025 (ฐาน)
เข็มที่ 2: 1 มกราคม + 28 วัน = 29 มกราคม 2025
เข็มที่ 3: 1 มกราคม + 168 วัน = 17 มิถุนายน 2025
```

### ตัวอย่างที่ 2: วัคซีน HPV (3 เข็ม)

**ข้อมูล:**
- เข็มแรก: 15 มกราคม 2025
- dose_intervals: `[28, 140]`

**การคำนวณ:**
```
เข็มที่ 1: 15 มกราคม 2025 (ฐาน)
เข็มที่ 2: 15 มกราคม + 28 วัน = 12 กุมภาพันธ์ 2025
เข็มที่ 3: 15 มกราคม + 140 วัน = 4 มิถุนายน 2025
```

### ตัวอย่างที่ 3: วัคซีนพิษสุนัขบ้า (5 เข็ม) - หลังแก้ไข

**ข้อมูล:**
- เข็มแรก: 1 มกราคม 2025
- dose_intervals: `[3, 7, 14, 28]` ← หลังแก้ไข

**การคำนวณ:**
```
เข็มที่ 1: 1 มกราคม 2025 (ฐาน)
เข็มที่ 2: 1 มกราคม + 3 วัน = 4 มกราคม 2025
เข็มที่ 3: 1 มกราคม + 7 วัน = 8 มกราคม 2025
เข็มที่ 4: 1 มกราคม + 14 วัน = 15 มกราคม 2025
เข็มที่ 5: 1 มกราคม + 28 วัน = 29 มกราคม 2025
```

---

## 🎯 กรณีพิเศษ: ผู้ป่วยฉีดล่าช้า

**สถานการณ์:**
- เข็มแรก: 1 มกราคม 2025 (ตามแผน)
- เข็มที่ 2: 10 มกราคม 2025 (ล่าช้า 1 วัน, ควรเป็น 9 มกราคม)
- วัคซีน: อีสุกอีใส (chickenpox), dose_intervals: `[28]`

**การคำนวณเข็มที่ 3:**
```
✅ วิธีถูก (นับจากเข็มแรก):
เข็มที่ 3 = 1 มกราคม + 28 วัน = 29 มกราคม 2025

❌ วิธีผิด (นับจากเข็มล่าสุด):
เข็มที่ 3 = 10 มกราคม + 28 วัน = 7 กุมภาพันธ์ 2025
```

**เหตุผล:**
- ระบบคำนวณตามแผนที่กำหนดไว้
- ไม่ปรับตามการฉีดจริง (เพราะอาจล่าช้าหรือเร็วกว่ากำหนด)
- แพทย์สามารถปรับเปลี่ยนได้ตามความเหมาะสม

---

## ✅ สรุป

### หลักการสำคัญ:
1. ✅ **นับจากเข็มแรกเป็นฐาน** - ไม่นับต่อจากเข็มล่าสุด
2. ✅ **dose_intervals เก็บระยะห่างจากเข็มแรก** - ไม่ใช่ระยะห่างระหว่างเข็มติดกัน
3. ✅ **โค้ดปัจจุบันคำนวณถูกต้อง** - ตามมาตรฐานทางการแพทย์
4. ⚠️ **ต้องแก้ไขข้อมูล vaccine_type = 'rabies'** - เปลี่ยน `[3,4,7,14]` เป็น `[3,7,14,28]`

### ไฟล์ที่ใช้การคำนวณถูกต้อง:
- ✅ `src/components/NextAppointments.tsx` (บรรทัด 248-259)
- ✅ `src/components/FullDoseScheduleModal.tsx` (บรรทัด 96-139)

### ไฟล์ที่ไม่ได้ใช้งาน:
- 📦 `src/lib/vaccineCalculationUtils.ts` - Utility module (ไม่ถูกเรียกใช้ในโค้ดจริง)

---

**วันที่สร้าง:** 28 พฤศจิกายน 2025
**เวอร์ชัน:** 1.0.0
**ผู้จัดทำ:** VCHome Hospital Development Team

**หมายเหตุ:** เอกสารนี้เป็นเอกสารเดียวที่เป็นมาตรฐานสำหรับการคำนวณวันนัดวัคซีนในระบบ เอกสารอื่นๆ ที่ขัดแย้งได้ถูกลบออกแล้ว
