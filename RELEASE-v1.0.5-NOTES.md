# Release Notes - Version 1.0.5

**วันที่:** 7 พฤศจิกายน 2568  
**เวอร์ชัน:** 1.0.5  
**ประเภท:** Major Update - Vaccine Calculation Standardization

---

## 🎯 สรุปการอัปเดต

เวอร์ชันนี้เป็นการอัปเดตสำคัญที่ปรับปรุงระบบการคำนวณวันนัดวัคซีนให้เป็นมาตรฐานเดียวกันทั้งระบบ โดยยึดข้อมูลจากตาราง `vaccine_schedules` ใน Supabase เป็น Single Source of Truth

---

## ✨ ฟีเจอร์ใหม่

### 1. 🔧 เครื่องมือแก้ไขนัดที่ไม่ตรง
**หน้าใหม่:** `/fix-appointments`

**ความสามารถ:**
- ✅ สแกนหานัดที่ไม่ตรงกับ vaccine_schedules อัตโนมัติ
- ✅ แสดงเปรียบเทียบ: นัดปัจจุบัน vs นัดที่ควรเป็น
- ✅ แสดงความแตกต่าง (จำนวนวัน)
- ✅ เลือกนัดที่ต้องการแก้ไข (checkbox)
- ✅ แก้ไขหลายนัดพร้อมกัน
- ✅ บันทึกหมายเหตุการเปลี่ยนแปลงอัตโนมัติ

**ประโยชน์:**
- แก้ไขนัดที่ฉีดไปแล้วแต่วันนัดไม่ตรงกับตาราง
- ประหยัดเวลาในการแก้ไขทีละนัดด้วยมือ
- ลดข้อผิดพลาดจากการคำนวณ

---

## 🔄 การปรับปรุงระบบคำนวณวัคซีน

### หลักการคำนวณใหม่:
```
next_dose_date = first_dose_date + Σ(dose_intervals[0] to dose_intervals[current_dose-1])
```

### ไฟล์ที่ได้รับการปรับปรุง:

#### 1. PatientNextAppointment.tsx
**ปัญหาเดิม:**
- ใช้ `latest_date` แทน `first_dose_date`
- ใช้ interval เดียวแทนการสะสม

**การแก้ไข:**
- ✅ เปลี่ยนเป็นคำนวณจาก `first_dose_date`
- ✅ สะสม intervals ทั้งหมดจนถึงโดสปัจจุบัน
- ✅ เพิ่ม logging ที่ละเอียดขึ้น

#### 2. VaccineDoseCalculator.tsx
**ปัญหาเดิม:**
- ใช้ `lastDoseDate` แทน `first_dose_date`
- ไม่มีฟิลด์ `firstDoseDate` ในฟอร์ม

**การแก้ไข:**
- ✅ เพิ่มฟิลด์ "วันที่ฉีดเข็มแรก"
- ✅ เปลี่ยนการคำนวณเป็นแบบสะสม intervals
- ✅ ปรับ UI grid จาก 3 เป็น 4 columns

#### 3. EditPatientAppointment.tsx
**ปัญหาเดิม:**
- ใช้ `lastDoseDate` แทน `first_dose_date`
- ไม่มีการดึงข้อมูลเข็มแรก

**การแก้ไข:**
- ✅ เปลี่ยนฟังก์ชัน `calculateNextDoseDate` เป็น async
- ✅ ดึงข้อมูล first_dose_date จาก database
- ✅ ใช้การสะสม intervals

---

## 📊 ผลการทดสอบ

### ✅ การทดสอบวัคซีนทั้งหมด: 8/8 ผ่าน (100%)

| วัคซีน | จำนวนเข็ม | Intervals | สถานะ |
|--------|-----------|-----------|-------|
| วัคซีนพิษสุนัขบ้า (Rabies) | 5 | [3, 4, 7, 14] | ✅ ผ่าน |
| วัคซีนไข้หวัดใหญ่ (Flu) | 2 | [365] | ✅ ผ่าน |
| วัคซีนป้องกันบาดทะยัก (Tetanus) | 3 | [28, 168] | ✅ ผ่าน |
| วัคซีนงูสวัด (Shingles) | 2 | [84] | ✅ ผ่าน |
| วัคซีนปอดอักเสบ (Pneumonia) | 2 | [56] | ✅ ผ่าน |
| วัคซีนไวรัสตับอักเสบบี (Hep B) | 3 | [28, 140] | ✅ ผ่าน |
| วัคซีนป้องกันมะเร็งปากมดลูก (HPV) | 3 | [28, 140] | ✅ ผ่าน |
| วัคซีนอีสุกอีใส (Chickenpox) | 2 | [28] | ✅ ผ่าน |

### ตัวอย่างการคำนวณ:

**วัคซีนพิษสุนัขบ้า (Rabies):**
- เข็มที่ 1: วันที่ 0 (วันแรก)
- เข็มที่ 2: 0 + 3 = วันที่ 3 ✅
- เข็มที่ 3: 0 + 3 + 4 = วันที่ 7 ✅
- เข็มที่ 4: 0 + 3 + 4 + 7 = วันที่ 14 ✅
- เข็มที่ 5: 0 + 3 + 4 + 7 + 14 = วันที่ 28 ✅

**วัคซีน HPV:**
- เข็มที่ 1: วันที่ 0 (วันแรก)
- เข็มที่ 2: 0 + 28 = วันที่ 28 (1 เดือน) ✅
- เข็มที่ 3: 0 + 28 + 140 = วันที่ 168 (6 เดือน) ✅

---

## 📁 ไฟล์ที่เพิ่ม/แก้ไข

### ไฟล์ใหม่:
- `src/components/FixIncorrectAppointments.tsx` - เครื่องมือแก้ไขนัด
- `src/pages/FixAppointmentsPage.tsx` - หน้าสำหรับใช้งาน
- `src/lib/vaccineCalculationUtils.ts` - Utility functions (มีอยู่แล้ว)
- `FIX-APPOINTMENTS-GUIDE.md` - คู่มือการใช้งาน
- `FIX-APPOINTMENTS-SUMMARY.md` - สรุปโดยละเอียด
- `VACCINE-CALCULATION-FIX-SUMMARY.md` - สรุปการแก้ไข
- `VACCINE-CALCULATION-TEST-REPORT.md` - รายงานผลการทดสอบ

### ไฟล์ที่แก้ไข:
- `src/components/PatientNextAppointment.tsx`
- `src/components/VaccineDoseCalculator.tsx`
- `src/components/EditPatientAppointment.tsx`
- `src/App.tsx` - เพิ่ม route `/fix-appointments`
- `package.json` - อัปเดตเวอร์ชันเป็น 1.0.5

---

## 🚀 วิธีติดตั้ง

### สำหรับผู้ใช้งานทั่วไป:

1. **ดาวน์โหลดไฟล์ติดตั้ง:**
   - `VCHome Hospital Setup 1.0.5.exe` (Installer)
   - หรือ `VCHome-Hospital-Portable.exe` (Portable)

2. **ติดตั้ง:**
   - รันไฟล์ `VCHome Hospital Setup 1.0.5.exe`
   - ทำตามขั้นตอนการติดตั้ง
   - โปรแกรมจะอัปเดตอัตโนมัติถ้ามีเวอร์ชันเก่าอยู่

3. **เริ่มใช้งาน:**
   - เปิดโปรแกรม VCHome Hospital
   - ระบบจะใช้การคำนวณแบบใหม่อัตโนมัติ

### สำหรับผู้ดูแลระบบ:

**ตรวจสอบและแก้ไขนัดที่ไม่ตรง:**
1. เปิดโปรแกรม
2. ไปที่เมนู "แก้ไขนัดที่ไม่ตรง" หรือพิมพ์ URL: `#/fix-appointments`
3. คลิก "สแกนนัดที่ไม่ตรง"
4. ตรวจสอบรายการที่พบ
5. เลือกนัดที่ต้องการแก้ไข
6. คลิก "แก้ไข X นัด"
7. แจ้งผู้ป่วยเกี่ยวกับการเปลี่ยนแปลง

---

## ⚠️ ข้อควรระวัง

### 1. การแก้ไขนัดที่มีอยู่
- ✅ ตรวจสอบว่าวันที่คำนวณได้สมเหตุสมผล
- ✅ ถ้าต่างกันมาก (>14 วัน) ควรตรวจสอบข้อมูลเข็มแรก
- ⚠️ **สำคัญ:** ต้องแจ้งผู้ป่วยทุกครั้งที่เปลี่ยนนัด

### 2. การใช้งานครั้งแรก
- ✅ ควรสแกนหานัดที่ไม่ตรงทันทีหลังอัปเดต
- ✅ แก้ไขนัดที่พบให้ตรงกับตาราง
- ✅ แจ้งผู้ป่วยเกี่ยวกับการเปลี่ยนแปลง

### 3. การสำรองข้อมูล
- ✅ ระบบจะบันทึกหมายเหตุการเปลี่ยนแปลงอัตโนมัติ
- ✅ ข้อมูลเดิมจะถูกบันทึกในฟิลด์ `notes`

---

## 📖 เอกสารประกอบ

1. **FIX-APPOINTMENTS-GUIDE.md** - คู่มือการใช้งานเครื่องมือแก้ไขนัด
2. **VACCINE-CALCULATION-FIX-SUMMARY.md** - สรุปการแก้ไขระบบคำนวณ
3. **VACCINE-CALCULATION-TEST-REPORT.md** - รายงานผลการทดสอบ

---

## 🔄 การอัปเดตอัตโนมัติ

โปรแกรมรองรับการอัปเดตอัตโนมัติผ่าน GitHub Releases:
- ✅ ตรวจสอบเวอร์ชันใหม่อัตโนมัติเมื่อเปิดโปรแกรม
- ✅ แจ้งเตือนเมื่อมีเวอร์ชันใหม่
- ✅ ดาวน์โหลดและติดตั้งอัตโนมัติ

---

## 🐛 Bug Fixes

- ✅ แก้ไขการคำนวณวันนัดที่ไม่สอดคล้องกันระหว่าง components
- ✅ แก้ไขปัญหาการคำนวณจาก latest_date แทน first_dose_date
- ✅ แก้ไขปัญหาการใช้ interval เดียวแทนการสะสม

---

## 📊 สถิติการเปลี่ยนแปลง

- **ไฟล์ที่เพิ่ม:** 7 ไฟล์
- **ไฟล์ที่แก้ไข:** 5 ไฟล์
- **บรรทัดโค้ดที่เพิ่ม:** ~1,500 บรรทัด
- **ฟีเจอร์ใหม่:** 1 ฟีเจอร์หลัก
- **การปรับปรุง:** 3 components
- **การทดสอบ:** 8 วัคซีน, 100% ผ่าน

---

## 🎉 สรุป

เวอร์ชัน 1.0.5 เป็นการอัปเดตสำคัญที่ปรับปรุงระบบการคำนวณวันนัดวัคซีนให้มีความแม่นยำและสอดคล้องกันทั้งระบบ พร้อมเครื่องมือใหม่สำหรับแก้ไขนัดที่ไม่ตรงอัตโนมัติ

**ผลลัพธ์:**
- ✅ การคำนวณแม่นยำ 100%
- ✅ ทุก component คำนวณแบบเดียวกัน
- ✅ ยึด vaccine_schedules เป็น Single Source of Truth
- ✅ มีเครื่องมือแก้ไขนัดที่ไม่ตรง
- ✅ ทดสอบผ่านทุกวัคซีน

---

**ติดต่อสนับสนุน:**
- GitHub: [Repository URL]
- Email: support@vchome-hospital.com
- โทร: 02-xxx-xxxx

**ขอบคุณที่ใช้งาน VCHome Hospital Management System!** 🏥
